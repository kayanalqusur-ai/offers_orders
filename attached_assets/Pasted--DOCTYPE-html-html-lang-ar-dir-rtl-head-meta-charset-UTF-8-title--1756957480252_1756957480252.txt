<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>عروض البيع - وسط الرياض</title>
  <link href="https://fonts.googleapis.com/css2?family=Cairo&display=swap" rel="stylesheet">
  <style>
    body{font-family:'Cairo',sans-serif;background:#ecf0f1;margin:0}
    nav{background:#ecf0f1;padding:10px 40px}
    nav ul{display:flex;gap:20px;list-style:none;margin:0;padding:0}
    nav a{color:#2c3e50;text-decoration:none;font-weight:bold;padding:8px 15px;border-radius:8px;transition:.3s}
    nav a:hover{background:#3498db;color:#fff}
    .navbar{background:#34495e;color:#fff;padding:15px;text-align:center;font-size:24px}
    .container{max-width:1200px;margin:30px auto;padding:20px}
    .card{background:#fff;border-radius:12px;padding:20px;box-shadow:0 4px 8px rgba(0,0,0,.05);margin-bottom:25px}
    .btn{padding:10px 14px;border:none;border-radius:6px;font-size:15px;cursor:pointer;text-decoration:none;display:inline-block}
    .btn-add{background:#27ae60;color:#fff}
    .btn-add:hover{background:#1e8449}
    .btn-primary{background:#2980b9;color:#fff}
    .btn-edit{background:#f39c12;color:#fff}
    .btn-delete{background:#c0392b;color:#fff}
    table{width:100%;border-collapse:collapse;margin-top:15px;font-size:14px}
    th,td{border:1px solid #ddd;padding:10px;text-align:center}
    th{background:#2980b9;color:#fff}
    tr:nth-child(even){background:#f8f9fa}
    .property-img{max-width:80px;border-radius:6px;margin:3px}
    form.inline{display:inline}
  </style>
</head>
<body>

<div class="navbar">📋 عروض البيع - وسط الرياض</div>

<nav>
    <ul>
        <li><a href="{{ url_for('dashboard') }}">الرئيسية</a></li>
        <li><a href="{{ url_for('list_employees') }}">الموظفين</a></li>
        <li><a href="{{ url_for('rentalm_offers') }}">عروض وسط الرياض للإيجار</a></li>
        <li><a href="{{ url_for('rentalw_offers') }}">عروض جنوب الرياض للإيجار</a></li>
        <li><a href="{{ url_for('salesm_offers') }}">عروض وسط الرياض للبيع</a></li>
        <li><a href="{{ url_for('salesw_offers') }}">عروض جنوب الرياض للبيع</a></li>
        <li><a href="{{ url_for('orders') }}">الطلبات</a></li>
        <li><a href="{{ url_for('view_logs') }}">عرض السجلات</a></li>
        <li><a href="{{ url_for('logout') }}">تسجيل الخروج</a></li>
    </ul>
</nav>

<div class="container">
  <div class="card">
    <a href="{{ url_for('add_salesm_offer') }}" class="btn btn-add">➕ إضافة عرض بيع جديد</a>
  </div>

  <div class="card">
    {% if offers %}
      <table>
        <thead>
          <tr>
            <th>الصورة</th>
            <th>نوع العقار</th>
            <th>الحي</th>
            <th>المساحة</th>
            <th>الدور</th>
            <th>الواجهة</th>
            <th>الشارع</th>
            <th>السوم</th>
            <th>حد البيع</th>
            <th>الموقع</th>
            <th>تفاصيل العقار</th>
            <th>اسم المسوق</th>
            <th>صفة المالك</th>
            <th>حالة العرض</th>
            <th>التحكم</th>
          </tr>
        </thead>
        <tbody>
          {% for offer in offers %}
          <tr>
               <td>
                {% if offer.images %}
                  <img src="{{ url_for('static', filename='uploads/' ~ offer.images[0]) }}" class="property-img" alt="صورة">
                {% else %}لا توجد صورة{% endif %}
              </td>
            <td>{{ offer.unit_type }}</td>
            <td>{{ offer.district }}</td>
            <td>{{ offer.area }} م²</td>
            <td>{{ offer.floor }}</td>
              <td>{{ offer.front }}</td>
              <td>{{ offer.street }}</td>
              <td>{{ offer.price }} ريال</td>
              <td>{{ offer.sale_limit }} ريال</td>
              <td><a href="{{ offer.location }}" target="_blank">🌍 عرض</a></td>
              <td>{{ offer.details }}</td>
              <td>{{ offer.marketer }}</td>
              <td>{{ offer.owner_type }}</td>
              <td>{{ offer.status }}</td>
           
              <td>
                <a href="{{ url_for('edit_salesm_offer', offer_id=offer.id) }}" class="btn btn-edit">✏️ تعديل</a>

                <form class="inline" method="POST" action="{{ url_for('delete_salesm_offer', offer_id=offer.id) }}" onsubmit="return confirm('هل أنت متأكد من حذف العرض؟');">
                  <button type="submit" class="btn btn-delete">🗑️ حذف</button>
                </form>
              </td>
            </tr>
          {% endfor %}
        </tbody>
      </table>
    {% else %}
      <p>لا توجد عروض حالياً.</p>
    {% endif %}
  </div>
</div>
</body>
</html>
